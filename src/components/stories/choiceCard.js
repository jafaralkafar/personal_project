import React from 'react'
import { connect } from 'react-redux'

const ChoiceCard = props => {

    return (
        <div>
            ChoiceCard
        </div>
    )
}

export default connect(null)(ChoiceCard)